MYSQL_ROOT_PASSWORD: "{{ lookup('amazon.aws_ssm', '/roboshop/sbx/mysql_root_password', decrypt=true) }}"

#########MYSQL_ROOT_PASSWORD: "{{ (lookup('amazon.aws.aws_secret', '/roboshop/dev/mysql_root_password') | from_json).MYSQL_ROOT_PASSWORD }}"
